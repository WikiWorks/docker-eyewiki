# EyeWiki Canasta Stack

* Put your Google certificate file at `eyewiki-google-key.json` (see more at https://www.mediawiki.org/wiki/Extension:GoogleAnalyticsMetrics)
* Copy `.env.example` to `.env` and alter as needed
* Run `docker compose up -d`
